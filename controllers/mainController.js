/*const controller = {
	index: (req, res) => {
		
		res.render('index.ejs')//productos(si se agrega alguna seccion, hay que hacer un filter, para llamarla al producto deseado)// faltaria tener un JSON de los productos para poder llamarlo en el controller y usarlo en la vista
        },
	search: (req, res) => {
		
	}
}

module.exports = controller */
